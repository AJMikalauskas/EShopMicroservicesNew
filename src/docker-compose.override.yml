version: '3.9'

services:
  catalogdb:
    container_name: catalogdb
    environment:
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=E2qLkqjTkxFzxuBKuQ00
        - POSTGRES_DB=CatalogDB
    restart: always
    ports:
        - "5432:5432"
    volumes:
        - postgres_catalog:/var/lib/postgresql/data/ 